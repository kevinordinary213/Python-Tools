```
import sys,random
import time
import itertools

def main():

  words = "1234567890"
  temp = itertools.permutation(word,2)
  password = open("dir.txt","a")

  for i in temp:
    passwords.write("".join(i))
    passwords.write("".join("\n"))
  passwords.close()


//会生成2位数字组合的字典


```

```

import sys,random
import time
import itertools

def main():

  words = "abcdefghijklmnopqrstuvwxyz"
  temp = itertools.permutation(word,4)
  password = open("dir.txt","a")

  for i in temp:
    passwords.write("".join(i))
    passwords.write("".join("\n"))
  passwords.close()

//生成4位字母字典

```
